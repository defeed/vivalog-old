%h2 Payouts
.table-responsive
  %table.table.bordered
    %thead
      %tr
        %th Name
        %th Work date
        %th Work
        %th Payment
        %th Amount
    %tbody
      - @project.entries.each do |entry|
        %tr
          %td= entry.user
          %td= moment(entry.worked_on)
          %td= I18n.t("work_types.#{entry.work_type}")
          %td= payment_for_entry(entry)
          %td= eur(entry.payout.amount)
